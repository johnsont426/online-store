<p>Back to <%= link_to "#{@item.category.name}", category_items_path(@item.category.id) %>
<h1><%= @item.name %></h1>
<img src="<%= @item.image_url %>" alt="picture of <%= @item.name %>">
<p><%= @item.description %></p>
<h4><%= @item.inventory %> left</h4>
<h4><%= number_to_currency(@item.price) %></h4>

<%= button_to 'Add to Cart', line_items_path(item_id: @item.id) unless current_user.nil? %>